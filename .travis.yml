language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - hhvm

before_script:
  - composer self-update
  - composer install

script:
  - phpunit
  - vendor/bin/phpcs --standard=PSR2 --encoding=utf-8 --ignore=vendor
